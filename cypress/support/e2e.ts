import '@badeball/cypress-cucumber-preprocessor';
import './commands';


// Limpieza del estado antes de cada prueba
beforeEach(() => {
  // Código de limpieza
});

// Hook después de cada prueba
afterEach(function () {
  // Código después de cada prueba
});